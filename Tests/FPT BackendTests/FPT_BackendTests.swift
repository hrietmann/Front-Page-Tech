    import XCTest
    @testable import FPT_Backend

    final class FPT_BackendTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(FPT_Backend().text, "Hello, World!")
        }
    }
